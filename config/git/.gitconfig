[user]
	name = Etienne Monier
	email = etienne.monier@csgroup.eu

[core]
	editor = nvim +3 +startinsert
	pager = cat

[commit]
	template = ~/.gitmessagecommit.txt

[alias]
    lg = log --branches --remotes --tags --graph --oneline --decorate

[init]
	defaultBranch = develop
